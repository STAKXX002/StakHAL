[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "stakhal-py"
version = "0.1.0"
description = "PyO3 bindings for stakhal-core analysis engine"
requires-python = ">=3.8"

[tool.maturin]
module-name = "stakhal_py"
